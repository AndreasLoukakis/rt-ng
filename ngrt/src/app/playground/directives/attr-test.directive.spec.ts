import { AttrTestDirective } from './attr-test.directive';

describe('AttrTestDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrTestDirective();
    expect(directive).toBeTruthy();
  });
});
