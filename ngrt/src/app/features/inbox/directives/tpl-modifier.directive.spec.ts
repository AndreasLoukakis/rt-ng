import { TplModifierDirective } from './tpl-modifier.directive';

describe('TplModifierDirective', () => {
  it('should create an instance', () => {
    const directive = new TplModifierDirective();
    expect(directive).toBeTruthy();
  });
});
